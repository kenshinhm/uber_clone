type Verification {
    id: Int!
    target: String! # password, phone
    payload: String!
    key: String!
    createdAt: String!
    updatedAt: String
}

type Query {
    verification: Verification
}
