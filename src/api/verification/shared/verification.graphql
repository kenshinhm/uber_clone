type Verification {
    id: Int!
    target: String! # password, phone
    payload: String!
    key: String!
    verified: Boolean!
    createdAt: String!
    updatedAt: String
}

type Query {
    verification: Verification
}
