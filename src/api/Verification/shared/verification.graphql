type Verification {
  id: Int!
  target: String! # password, phone
  payload: String!
  key: String!
  used: Boolean!
  user: User!
  createdAt: String!
  updatedAt: String!
}

type Query {
  verification: Verification
}
